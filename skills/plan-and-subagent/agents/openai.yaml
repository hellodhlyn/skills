interface:
  display_name: "Plan and Subagent"
  short_description: "Plan, delegate, externally review, commit, and create a pull request"
  default_prompt: "Use $plan-and-subagent to plan, implement, validate, externally review, commit, and create a PR for this code change."
