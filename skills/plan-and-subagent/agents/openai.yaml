interface:
  display_name: "Plan and Subagent"
  short_description: "Plan in the primary agent, delegate to an implementer subagent, require an independent external review"
  default_prompt: "Use $plan-and-subagent to plan this code change, delegate implementation to the configured implementer subagent (default: Luna at max reasoning), validate and review it directly, then complete the required external review (default: OpenCode reviewer agent at max variant)."
