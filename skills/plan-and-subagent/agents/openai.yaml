interface:
  display_name: "Plan and Subagent"
  short_description: "Plan, delegate, review, and deliver with your environment"
  default_prompt: "Use $plan-and-subagent with my configured environment to plan this change, delegate implementation, validate, independently review, and deliver the agreed result."
